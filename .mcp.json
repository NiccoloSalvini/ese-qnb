{
  "mcpServers": {
    "moodle": {
      "command": "npx",
      "args": ["-y", "github:NiccoloSalvini/mcp-moodle-teacher"],
      "env": {
        "MOODLE_URL": "${MOODLE_URL}",
        "MOODLE_TOKEN": "${MOODLE_TOKEN}"
      }
    }
  }
}
